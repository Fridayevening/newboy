# NB-009 Handoff

- Owner: Codex
- Base commit: `31a8507`
- Status: review
- Canonical source: `frontend/src/components/desktop/portfolioContent.ts`
- Archive target: `/Users/wangyanfei/workspace/coding-workspace/portfolio/yanfei-portfolio`

## Goal

Synchronise the approved NewBoy Work and Research facts into the old portfolio without reducing its long-form presentation or allowing old copy to overwrite NewBoy.

## Direction and boundaries

- Synchronisation is one way only: NewBoy → old portfolio.
- Preserve the old site's visual layout, evidence-cleared images and longer case-study structure.
- Correct withdrawn metrics, unsupported causal claims, excessive individual attribution and implemented-product claims that are only prototype concepts.
- Do not modify or commit unrelated pre-existing changes in the old repository.
- Do not commit, push or deploy either repository without Yanfei's explicit approval.

## Modified archive files

- `src/data/workStories.ts`
- `src/data/researchStories.ts`
- `src/components/research/ResearchDetail.tsx`

### Where these changes landed

Tracked down on 2026-09-23 with `git log -S` in the archive repository: the archive reconciliation landed in **`e2d55a4`** (2026-09-19, "feat: rebuild as a retro OS shell and sync Work/Research copy with the reviewed source"), not in a commit named after this task.

```bash
git -C <archive-repo> log -S'seven-person' --oneline -- src/data/workStories.ts
git -C <archive-repo> log -S'2,409'         --oneline -- src/data/workStories.ts
```

Both return `e2d55a4`. `43.8` never appeared in `src/data/researchStories.ts`, so that metric was not removed from this file.

Note for future archive-sync tasks: land the work in its own commit so the task ID is traceable from `git log` without a pickaxe search.

## Reconciliation completed

- Replaced withdrawn Uspace figures with 2,409 enterprise organisations, 29K portfolio MAU, 16 countries and six major releases using their correct portfolio scope.
- Corrected GDPR, temperature-screening, Henan Yuzi, XIZI, pricing and team-attribution language.
- Removed unsupported module and release counts from the China case while preserving verified workflow breadth.
- Reframed Operations Analytics around the confirmed catalogue, supplier, delivery, defect, Tableau, shortage and hardware/software-support signals.
- Scoped KreAI to a seven-person team and the confirmed ten-customer, two-version, two-week directional comparison from approximately 30% to 38% completion.
- Corrected healthcare research to a nine-person survey plus one separate expert interview with Dr Grace and removed the unsupported `6/9` figure.
- Removed the unconfirmed Lawmate `43.8%` metric, eight-person attribution and implemented encrypted-chat claim.

## Verification

- `npm run typecheck`: passed.
- `npm run build`: passed with Vite 5.4.21.
- `npm run lint`: passed.
- `git diff --check`: passed.
- Withdrawn-claim scan passed for the modified Work and Research source files.

## Existing unrelated changes

The archive repository already contained uncommitted edits in `TODO.md` and `documents/TECHNICAL.md`. NB-009 did not modify them and they must not be included in a content-sync commit.

## Remaining risk

The old site's `src/components/about/AboutMe.tsx` still contains `294K MAU` and a six-module claim. About is outside the authorised Work/Research scope and was not changed. It should be handled as a separate fact-alignment task if Yanfei wants the entire archive site to be internally consistent.

## Acceptance

Open every Work and Research case in the old site and verify that the existing visual evidence still renders, the revised narratives remain complete, and the corrected facts match NewBoy. Keep this task in `review` until Yanfei accepts the browser result.
